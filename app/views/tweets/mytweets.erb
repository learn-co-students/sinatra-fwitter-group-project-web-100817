<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Tweets by <%= @user.username %></title>
  </head>
  <body>

    <div class="mw9 center ph3-ns">
      <div class="cf ph2-ns">
        <div class="fl w-100 pa2">
          <h1><%= @user.username %>'s Tweets</h1>
        </div>

    <% @tweets.each do |tweet| %>
    <div class="w-80 pa1 center">
      <div class="bg-white pv1 bb">
        <p class="f6 teal"><%= tweet.user.username %> said: </p>
        <p class="f5"><%= tweet.content %></p>
      </div>
    </div>
    <% end %>
  </div>
</div>

      </div>
    </div>
  </body>
</html>
